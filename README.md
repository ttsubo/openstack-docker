## Docker Image Build

```
$ docker build -t ttsubo/keystone:1.0.0 docker/keystone/.
$ docker build -t ttsubo/glance:1.0.0 docker/glance/.
$ docker build -t ttsubo/nova:1.0.0 docker/nova/.
$ docker build -t ttsubo/neutron:1.0.0 docker/neutron/.
```
